#include<bits/stdc++.h>
using namespace std;
int main()
{
    float h, m, d;
    char c;
    while(cin>>h>>c>>m && h!=0 || m!=0)
    {
        h=(h*30)+(m/2);
        m=m*6;
        d=abs(h-m);
        d>180 ? printf("%.3f\n",360-d) : printf("%.3f\n",d);
    }
    return 0;
}
